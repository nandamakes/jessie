import Phaser from 'phaser';

// Phase 3 — Grid of plots, generators, offline catch-up income
export class TycoonScene extends Phaser.Scene {
  constructor() {
    super({ key: 'TycoonScene' });
  }

  create() {
    // stub
  }
}
