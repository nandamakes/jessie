import Phaser from 'phaser';

// Phase 2 — Side-scroll platformer, checkpoints, hazards, finish line
export class ObbyScene extends Phaser.Scene {
  constructor() {
    super({ key: 'ObbyScene' });
  }

  create() {
    // stub
  }
}
